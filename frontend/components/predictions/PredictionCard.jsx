'use client';import{useState}from'react';import GlowCard from'@/components/ui/GlowCard';import ConfidenceRing from'./ConfidenceRing';import EdgeMeter from'./EdgeMeter';import ReasoningPanel from'./ReasoningPanel';export default function PredictionCard({prediction,onAdd}){const[open,setOpen]=useState(false);return <GlowCard className="p-5"><div className="flex items-start justify-between gap-4"><div><div className="mb-2 flex gap-2"><span className="tag">{prediction.sport}</span><span className="tag">{prediction.seasonMode}</span></div><h3 className="text-xl font-bold">{prediction.player}</h3><p className="text-sm text-gray-400">{prediction.game} · {prediction.market}</p></div><ConfidenceRing value={prediction.confidence}/></div><div className="my-5 grid grid-cols-3 gap-3 text-sm"><Metric label="Line" value={prediction.line}/><Metric label="Projection" value={prediction.projection}/><Metric label="EV" value={`+${prediction.expectedValue}%`}/></div><EdgeMeter value={prediction.edge}/><div className="mt-5 flex flex-wrap gap-2"><button className="btn btn-primary focus-ring" onClick={()=>onAdd?.(prediction)}>Add to parlay</button><button className="btn btn-glass focus-ring" onClick={()=>setOpen(!open)} aria-expanded={open}>{open?'Hide':'Reasoning'}</button><span className="tag">{prediction.recommendation} {prediction.odds}</span></div>{open&&<ReasoningPanel prediction={prediction}/>}</GlowCard>}
function Metric({label,value}){return <div className="rounded-2xl bg-white/[.04] p-3"><p className="text-gray-500">{label}</p><p className="font-bold">{value}</p></div>}
