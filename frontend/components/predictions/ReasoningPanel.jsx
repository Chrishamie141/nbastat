export default function ReasoningPanel({prediction}){const items=['Recent form trending above median usage','Opponent matchup creates exploitable pace and role fit','Usage trend stable across comparable game scripts','Injury effect included with availability downgrade checks','Weather and venue context considered for NFL markets','Line movement monitored against model fair line','Historical hit rate separated from sample mode','Expected playing time and preseason role fields reviewed'];return <div className="mt-4 grid gap-2 text-sm text-gray-300">{items.map(i=><p key={i} className="rounded-2xl bg-white/[.04] p-3">{i}</p>)}<p className="tag w-fit">{prediction?.preseason?.coachNotes}</p></div>}
