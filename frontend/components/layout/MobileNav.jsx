'use client';import Link from'next/link';import{usePathname}from'next/navigation';import{Home,Search,History,User}from'lucide-react';import{useAuth}from'@/components/auth/AuthProvider';
export default function MobileNav(){const path=usePathname();const{isAuthenticated}=useAuth();if(!isAuthenticated)return null;const links=[['/dashboard',Home,'Dashboard'],['/analyze',Search,'Analyze'],['/history',History,'History'],['/account',User,'Account']];return <div className="fixed bottom-4 left-1/2 z-40 flex w-[min(420px,calc(100%-24px))] -translate-x-1/2 justify-around rounded-3xl border border-white/10 bg-black/70 p-2 backdrop-blur md:hidden">{links.map(([href,Icon,label])=><Link key={href} href={href} className={`flex flex-col items-center rounded-2xl px-3 py-2 text-xs ${path===href?'bg-white/10 text-white':'text-gray-400'}`}><Icon size={18}/>{label}</Link>)}</div>}
