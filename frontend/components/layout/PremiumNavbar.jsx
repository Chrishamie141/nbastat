'use client';import Link from'next/link';import{usePathname}from'next/navigation';import{BarChart3,Menu}from'lucide-react';const links=['dashboard','predictions','parlays','performance','fantasy'];export default function PremiumNavbar(){const path=usePathname();return <header className="fixed left-1/2 top-4 z-40 w-[min(1120px,calc(100%-24px))] -translate-x-1/2"><nav className="glass flex items-center justify-between rounded-[24px] px-4 py-3"><Link href="/" className="flex items-center gap-2 font-black tracking-[-.04em]"><BarChart3 className="text-violet-300"/> APEX<span className="text-violet-300">EDGE</span></Link><div className="hidden gap-1 md:flex">{links.map(l=><Link key={l} href={`/${l}`} className={`rounded-full px-3 py-2 text-sm capitalize transition ${path.includes(l)?'bg-white/10 text-white':'text-gray-400 hover:text-white'}`}>{l}</Link>)}</div><Link href="/account" className="btn btn-primary hidden px-4 py-2 md:block">Account</Link><button className="md:hidden" aria-label="Open menu"><Menu/></button></nav></header>}
