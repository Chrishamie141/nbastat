'use client';import Link from'next/link';import{usePathname}from'next/navigation';import{BarChart3,LogOut,Menu}from'lucide-react';import{useAuth}from'@/components/auth/AuthProvider';
export default function PremiumNavbar(){const path=usePathname();const{isAuthenticated,user,logout}=useAuth();const authed=['dashboard','analyze','history','performance','account'];return <header className="fixed left-1/2 top-4 z-40 w-[min(1120px,calc(100%-24px))] -translate-x-1/2"><nav className="glass flex items-center justify-between rounded-[24px] px-4 py-3"><Link href="/" className="flex items-center gap-2 font-black tracking-[-.04em]"><BarChart3 className="text-violet-300"/> APEX<span className="text-violet-300">EDGE</span></Link>{isAuthenticated?<div className="hidden gap-1 md:flex">{authed.map(l=><Link key={l} href={`/${l}`} className={`rounded-full px-3 py-2 text-sm capitalize transition ${path.includes(l)?'bg-white/10 text-white':'text-gray-400 hover:text-white'}`}>{l}</Link>)}</div>:<div className="hidden items-center gap-1 md:flex"><Link href="/#how" className="rounded-full px-3 py-2 text-sm text-gray-400 hover:text-white">How It Works</Link><Link href="/login" className="rounded-full px-3 py-2 text-sm text-gray-400 hover:text-white">Log In</Link><Link href="/register" className="btn btn-primary px-4 py-2">Get Started</Link></div>}{isAuthenticated?<button onClick={logout} className="hidden items-center gap-2 rounded-full bg-white/10 px-3 py-2 text-sm text-gray-300 hover:text-white md:flex"><span>{user?.name}</span><LogOut size={16}/></button>:<button className="md:hidden" aria-label="Open menu"><Menu/></button>}</nav></header>}
