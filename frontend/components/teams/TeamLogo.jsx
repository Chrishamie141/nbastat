'use client';import{useState}from'react';
export default function TeamLogo({team,size=44}){const[bad,setBad]=useState(false);const abbr=team?.abbreviation||'TBD';const src=!bad&&team?.logoUrl;const style={width:size,height:size};return <div style={style} className="flex shrink-0 items-center justify-center overflow-hidden rounded-full border border-white/10 bg-slate-950/70 text-xs font-black text-cyan-100 shadow-inner" aria-label={`${team?.name||abbr} logo`}>{src?<img src={src} alt={`${team?.name||abbr} logo`} width={size} height={size} className="h-full w-full object-contain p-1" onError={()=>setBad(true)}/>:<span>{abbr}</span>}</div>}
