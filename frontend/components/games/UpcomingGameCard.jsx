'use client';import TeamLogo from'@/components/teams/TeamLogo';
function formatGameTime(iso,status){if(['postponed','cancelled'].includes(status))return status[0].toUpperCase()+status.slice(1);if(status&&status!=='scheduled')return status==='live'?'Live':status;const d=new Date(iso);if(Number.isNaN(d.getTime()))return'Time TBD';const now=new Date();const start=new Date(now.getFullYear(),now.getMonth(),now.getDate());const day=new Date(d.getFullYear(),d.getMonth(),d.getDate());const diff=Math.round((day-start)/86400000);const dayLabel=diff===0?'Today':diff===1?'Tomorrow':new Intl.DateTimeFormat(undefined,{weekday:'short',month:'short',day:'numeric'}).format(d);const time=new Intl.DateTimeFormat(undefined,{hour:'numeric',minute:'2-digit',timeZoneName:'short'}).format(d);return `${dayLabel} · ${time}`}
function phaseLabel(g){return `${(g.league||'').toUpperCase()} ${(g.seasonPhase||'').replace('_',' ')}`}
export {formatGameTime};
export default function UpcomingGameCard({game,featured=false}){if(!game)return null;return <article className={`rounded-3xl border ${featured?'border-cyan-300/40 bg-cyan-300/10':'border-white/10 bg-white/[.04]'} p-4`}><div className="flex items-center justify-between gap-3"><span className="tag w-fit">{phaseLabel(game)}</span>{game.watchScore>0&&<span className="rounded-full bg-white/10 px-2 py-1 text-xs text-cyan-100">Watch {game.watchScore}</span>}</div><div className="mt-4 grid gap-3"><TeamRow label="Away" team={game.awayTeam}/><TeamRow label="Home" team={game.homeTeam}/></div><p className="mt-4 font-semibold text-cyan-50">{formatGameTime(game.startTimeUtc,game.status)}</p>{game.venue&&<p className="mt-1 text-sm text-slate-300">{game.venue}</p>}{game.broadcast?.length>0&&<p className="mt-1 text-sm text-slate-400">Broadcast: {game.broadcast.join(', ')}</p>}{featured&&game.watchReasons?.length>0&&<ul className="mt-3 flex flex-wrap gap-2">{game.watchReasons.map(r=><li key={r} className="rounded-full bg-white/10 px-2 py-1 text-xs text-slate-200">{r}</li>)}</ul>}</article>}
function TeamRow({label,team}){return <div className="flex items-center gap-3"><TeamLogo team={team}/><div><p className="text-xs uppercase tracking-wide text-slate-500">{label}</p><p className="font-bold"><span className="text-cyan-100">{team?.abbreviation}</span> <span className="text-slate-200">{team?.name}</span></p></div></div>}
