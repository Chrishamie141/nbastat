'use client';import{useEffect,useMemo,useRef,useState}from'react';import TeamLogo from'@/components/teams/TeamLogo';import{api}from'@/lib/api';
export default function TeamSelector({league='nfl',value='',onChange}){const[teams,setTeams]=useState([]),[open,setOpen]=useState(false),[query,setQuery]=useState(''),[active,setActive]=useState(0);const box=useRef(null);useEffect(()=>{api.teams(league).then(r=>setTeams(r.teams||[])).catch(()=>setTeams([]))},[league]);useEffect(()=>{const f=e=>{if(box.current&&!box.current.contains(e.target))setOpen(false)};document.addEventListener('mousedown',f);return()=>document.removeEventListener('mousedown',f)},[]);const selected=teams.find(t=>t.abbreviation===value);const filtered=useMemo(()=>{const q=query.toLowerCase().trim();return teams.filter(t=>!q||[t.name,t.city,t.nickname,t.abbreviation].some(x=>(x||'').toLowerCase().includes(q))).slice(0,12)},[teams,query]);function choose(team){onChange(team?.abbreviation||'');setQuery('');setOpen(false)}function key(e){if(!open&&(e.key==='ArrowDown'||e.key==='Enter')){setOpen(true);return}if(e.key==='ArrowDown'){e.preventDefault();setActive(a=>Math.min(a+1,filtered.length-1))}if(e.key==='ArrowUp'){e.preventDefault();setActive(a=>Math.max(a-1,0))}if(e.key==='Enter'){e.preventDefault();choose(filtered[active])}if(e.key==='Escape')setOpen(false)}return <div ref={box} className="relative"><label className="mb-2 block text-sm font-semibold text-slate-200" htmlFor="team-filter">Team filter</label><div className="flex items-center gap-2 rounded-2xl border border-white/10 bg-white/5 px-3 py-2 focus-within:outline focus-within:outline-2 focus-within:outline-cyan-300">{selected&&<TeamLogo team={selected} size={32}/>}<input id="team-filter" role="combobox" aria-expanded={open} aria-controls="team-filter-list" aria-autocomplete="list" placeholder={selected?`${selected.name} — ${selected.abbreviation}`:'All teams'} value={query} onFocus={()=>setOpen(true)} onKeyDown={key} onChange={e=>{setQuery(e.target.value);setOpen(true);setActive(0)}} className="min-w-0 flex-1 bg-transparent py-2 text-slate-100 placeholder:text-slate-400 focus:outline-none" />{(selected||query)&&<button type="button" onClick={()=>choose(null)} className="rounded-full px-2 py-1 text-sm text-slate-300 hover:bg-white/10">Clear</button>}</div>{open&&<div id="team-filter-list" role="listbox" className="absolute z-30 mt-2 max-h-72 w-full overflow-auto rounded-2xl border border-white/10 bg-slate-950 p-2 shadow-2xl"><button type="button" role="option" aria-selected={!value} onClick={()=>choose(null)} className="w-full rounded-xl px-3 py-2 text-left text-slate-100 hover:bg-white/10">All teams</button>{filtered.map((t,i)=><button type="button" key={t.abbreviation} role="option" aria-selected={i===active} onMouseEnter={()=>setActive(i)} onClick={()=>choose(t)} className={`flex w-full items-center gap-3 rounded-xl px-3 py-2 text-left ${i===active?'bg-cyan-300/15':'hover:bg-white/10'}`}><TeamLogo team={t} size={32}/><span><span className="block text-sm font-semibold text-slate-100">{t.name}</span><span className="text-xs text-slate-400">{t.abbreviation}</span></span></button>)}</div>}</div>}
