'use client';
const LEVELS=[['SAFE','Safe','Lower variance, higher-confidence legs, and more conservative thresholds.'],['BALANCED','Balanced','Mix of confidence and potential return. Default selection.'],['AGGRESSIVE','Aggressive','Higher variance, larger potential return, and more selective edge requirements.']];
export default function RiskLevelSelector({value='BALANCED',onChange}){function key(e,i){const keys=['ArrowRight','ArrowDown','ArrowLeft','ArrowUp'];if(!keys.includes(e.key))return;e.preventDefault();const dir=e.key==='ArrowRight'||e.key==='ArrowDown'?1:-1;onChange(LEVELS[(i+dir+LEVELS.length)%LEVELS.length][0]);}return <div role="radiogroup" aria-label="Risk level" className="grid gap-3 md:grid-cols-3">{LEVELS.map(([val,label,desc],i)=>{const selected=value===val;return <button key={val} type="button" role="radio" aria-checked={selected} tabIndex={selected?0:-1} onKeyDown={e=>key(e,i)} onClick={()=>onChange(val)} className={`rounded-2xl border p-4 text-left transition focus:outline focus:outline-2 focus:outline-cyan-300 ${selected?'border-cyan-300 bg-cyan-300/15 shadow-[0_0_24px_rgba(34,211,238,.18)]':'border-white/10 bg-white/5 hover:bg-white/10'}`}><span className="block font-bold text-slate-50">{label}</span><span className="mt-1 block text-sm leading-5 text-slate-300">{desc}</span></button>})}</div>}
