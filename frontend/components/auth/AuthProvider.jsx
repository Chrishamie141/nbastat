'use client';
import {createContext,useContext,useEffect,useMemo,useState}from 'react';import {usePathname,useRouter}from'next/navigation';import{api}from'@/lib/api';
const AuthContext=createContext(null);const protectedRoutes=['/dashboard','/analyze','/history','/performance','/account','/parlays','/fantasy','/players','/games','/predictions'];
export function AuthProvider({children}){const[user,setUser]=useState(null);const[ready,setReady]=useState(false);const router=useRouter();const path=usePathname();useEffect(()=>{api.auth.me().then(d=>setUser(d.user)).catch(()=>setUser(null)).finally(()=>setReady(true))},[]);useEffect(()=>{if(!ready)return;if(!user&&protectedRoutes.some(r=>path?.startsWith(r)))router.replace('/login')},[ready,user,path,router]);const register=async(form)=>{if(form.password!==form.confirm)throw new Error('Passwords do not match.');if((form.password||'').length<8)throw new Error('Password must contain at least 8 characters.');const d=await api.auth.register({name:form.name,email:form.email,password:form.password});setUser(d.user);return d};const login=async(form)=>{const d=await api.auth.login({email:form.email,password:form.password});setUser(d.user);return d};const logout=async()=>{try{await api.auth.logout()}finally{setUser(null);router.replace('/login')}};const value=useMemo(()=>({user,ready,loading:!ready,isAuthenticated:!!user,register,login,logout}),[user,ready]);return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>}
export function useAuth(){const ctx=useContext(AuthContext);if(!ctx)throw new Error('useAuth must be used within AuthProvider');return ctx}
