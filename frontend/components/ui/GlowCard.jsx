'use client';import{useState}from'react';export default function GlowCard({children,className='',selected=false}){const[pos,setPos]=useState({x:50,y:50});return <div onMouseMove={e=>{const r=e.currentTarget.getBoundingClientRect();setPos({x:((e.clientX-r.left)/r.width)*100,y:((e.clientY-r.top)/r.height)*100})}} className={`glass premium-card relative overflow-hidden ${selected?'ring-1 ring-violet-400/70':''} ${className}`}><div className="pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-300 hover:opacity-100" style={{background:`radial-gradient(circle at ${pos.x}% ${pos.y}%, rgba(139,92,246,.18), transparent 34%)`}}/><div className="relative">{children}</div></div>}
