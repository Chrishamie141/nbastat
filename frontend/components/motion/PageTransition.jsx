'use client';import{motion}from'framer-motion';import{ease}from'@/lib/motion';export default function PageTransition({children}){return <motion.main initial={{opacity:0,y:10,filter:'blur(6px)'}} animate={{opacity:1,y:0,filter:'blur(0px)'}} exit={{opacity:0,y:-6}} transition={{duration:.38,ease}}>{children}</motion.main>}
