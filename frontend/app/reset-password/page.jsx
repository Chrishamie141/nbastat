'use client';
import Link from 'next/link';
import {useState} from 'react';
import GlowCard from '@/components/ui/GlowCard';
import SmartBetSportsLogo from '@/components/branding/SmartBetSportsLogo';
import {api} from '@/lib/api';
export default function ResetPassword(){const[form,setForm]=useState({email:'',code:'',password:'',confirm:''});const[error,setError]=useState('');const[done,setDone]=useState(false);const[loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setError('');if(form.password!==form.confirm){setError('Passwords do not match.');return}setLoading(true);try{await api.auth.resetPassword({email:form.email,code:form.code,password:form.password});setDone(true)}catch(ex){setError(ex.message||'Unable to reset password.')}finally{setLoading(false)}}const input='focus-ring rounded-xl border border-white/10 bg-white/5 px-4 py-2.5';return <main className="mx-auto flex min-h-[calc(100vh-140px)] max-w-md items-center px-6 py-8"><GlowCard className="w-full p-6"><SmartBetSportsLogo size={34}/><h1 className="mt-4 text-3xl font-black">Reset password</h1>{done?<><p className="mt-4 text-cyan-100">Your password was changed and older sessions were signed out.</p><Link className="btn btn-primary mt-5 inline-flex" href="/login">Log In</Link></>:<form onSubmit={submit} className="mt-5 grid gap-3"><label className="grid gap-1 text-sm font-semibold">Email<input required type="email" autoComplete="email" className={input} value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="grid gap-1 text-sm font-semibold">Reset code<input required minLength={16} autoComplete="one-time-code" className={input} value={form.code} onChange={e=>setForm({...form,code:e.target.value})}/></label><label className="grid gap-1 text-sm font-semibold">New password<input required minLength={8} type="password" autoComplete="new-password" className={input} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><label className="grid gap-1 text-sm font-semibold">Confirm password<input required minLength={8} type="password" autoComplete="new-password" className={input} value={form.confirm} onChange={e=>setForm({...form,confirm:e.target.value})}/></label>{error&&<p className="rounded-xl bg-red-500/10 p-3 text-sm text-red-200">{error}</p>}<button disabled={loading} className="btn btn-primary disabled:opacity-60">{loading?'Resetting…':'Reset Password'}</button></form>}<Link className="mt-4 inline-block text-sm text-cyan-200" href="/login">Back to login</Link></GlowCard></main>}
