'use client';import{useState}from'react';import{demoPredictions}from'@/lib/api';import PredictionCard from'@/components/predictions/PredictionCard';import ParlaySlip from'@/components/parlays/ParlaySlip';export default function Parlays(){const[legs,setLegs]=useState([]);const add=p=>setLegs(l=>l.find(x=>x.id===p.id)?l:[...l,p]);return <main className="mx-auto max-w-7xl px-6 pt-32"><h1 className="text-5xl font-black tracking-[-.06em]">Parlay builder</h1><p className="demo-banner mt-4 rounded-2xl p-3 text-sm">DEMO / SAMPLE DATA — NOT FOR LIVE WAGERING</p><div className="mt-8 grid gap-6 lg:grid-cols-[1fr_380px]"><div className="grid gap-5">{demoPredictions.items.map(p=><PredictionCard key={p.id} prediction={p} onAdd={add}/>)}</div><ParlaySlip legs={legs} onRemove={id=>setLegs(l=>l.filter(x=>x.id!==id))}/></div></main>}
