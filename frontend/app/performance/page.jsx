'use client';import{useEffect,useState}from'react';import{LineChart,Line,ResponsiveContainer,XAxis,YAxis,Tooltip}from'recharts';import SubscriptionGuard from'@/components/auth/SubscriptionGuard';import GlowCard from'@/components/ui/GlowCard';import{api}from'@/lib/api';
export default function Performance(){return <SubscriptionGuard><Inner/></SubscriptionGuard>}
function Inner(){const[data,setData]=useState(null);useEffect(()=>{api.performance().then(setData).catch(()=>setData({metrics:{},series:[]}))},[]);const metrics=data?.metrics||{};const names=['NFL record','NFL accuracy','NFL graded predictions','NBA accuracy','Safe parlay performance','Balanced parlay performance','Aggressive parlay performance'];return <main className="mx-auto min-h-screen max-w-6xl px-6 pt-20"><h1 className="text-5xl font-black tracking-[-.06em]">Performance & grading</h1><p className="mt-2 text-gray-400">Grade saved predictions automatically from verified final results. Frozen winner predictions and tracked wagers are reported separately.</p><div className="mt-6 grid gap-4 md:grid-cols-3">{names.map(n=><GlowCard key={n} className="p-5"><p className="text-sm text-gray-400">{n}</p><p className="mt-2 text-2xl font-black">{metrics[n]??(n.toLowerCase().includes('parlay')?'No graded parlays yet.':'Not enough graded predictions to calculate this metric.')}</p></GlowCard>)}</div><GlowCard className="mt-6 p-6"><h2 className="text-2xl font-bold">Accuracy over time</h2>{data?.series?.length?<div className="mt-4 h-64"><ResponsiveContainer><LineChart data={data.series}><XAxis dataKey="date"/><YAxis/><Tooltip/><Line dataKey="accuracy" stroke="#a78bfa"/></LineChart></ResponsiveContainer></div>:<p className="mt-4 text-gray-400">Weekly trend appears after multiple graded slates. The current record above is still authoritative.</p>}</GlowCard></main>}
