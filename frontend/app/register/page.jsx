'use client';import Link from'next/link';import{useState}from'react';import{useRouter}from'next/navigation';import GlowCard from'@/components/ui/GlowCard';import{useAuth}from'@/components/auth/AuthProvider';import SmartBetSportsLogo from'@/components/branding/SmartBetSportsLogo';
export default function Register(){const{register}=useAuth();const router=useRouter();const[form,setForm]=useState({name:'',email:'',password:'',confirm:''});const[err,setErr]=useState('');const[loading,setLoading]=useState(false);const[show,setShow]=useState(false);async function submit(e){e.preventDefault();setErr('');if(form.password!==form.confirm){setErr('Passwords do not match.');return}if(form.password.length<8){setErr('Password must contain at least 8 characters.');return}setLoading(true);try{await register(form);router.push('/dashboard')}catch(ex){setErr(ex.message||'Unable to connect to the server.')}finally{setLoading(false)}}const input='focus-ring rounded-xl border border-white/10 bg-white/5 px-4 py-2.5';return <main className="mx-auto flex min-h-[calc(100vh-140px)] max-w-md items-center px-6 py-8"><GlowCard className="w-full p-6"><SmartBetSportsLogo size={34}/><h1 className="mt-4 text-3xl font-black tracking-[-.05em]">Create account</h1><form onSubmit={submit} className="mt-5 grid gap-3"><label className="grid gap-1 text-sm font-semibold">Name<input required autoComplete="name" className={input} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label className="grid gap-1 text-sm font-semibold">Email<input required type="email" autoComplete="email" className={input} value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="grid gap-1 text-sm font-semibold">Password<span className="flex rounded-xl border border-white/10 bg-white/5 focus-within:outline focus-within:outline-2 focus-within:outline-cyan-300"><input required minLength={8} type={show?'text':'password'} autoComplete="new-password" className="min-w-0 flex-1 bg-transparent px-4 py-2.5 outline-none" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button type="button" className="px-3 text-xs text-cyan-200" onClick={()=>setShow(!show)}>{show?'Hide':'Show'}</button></span></label><label className="grid gap-1 text-sm font-semibold">Confirm password<input required minLength={8} type={show?'text':'password'} autoComplete="new-password" className={input} value={form.confirm} onChange={e=>setForm({...form,confirm:e.target.value})}/></label>{err&&<p className="rounded-xl bg-red-500/10 p-3 text-sm text-red-200">{err}</p>}<button disabled={loading} className="btn btn-primary disabled:opacity-60">{loading?'Creating account…':'Create Account'}</button></form><p className="mt-4 text-sm text-slate-400">Already have an account? <Link className="text-cyan-200" href="/login">Log In</Link></p></GlowCard></main>}
