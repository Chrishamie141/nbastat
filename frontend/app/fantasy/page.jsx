import{fetchFantasy}from'@/lib/api';import GlowCard from'@/components/ui/GlowCard';export default async function Fantasy(){const f=await fetchFantasy();return <main className="mx-auto max-w-7xl px-6 pt-32"><h1 className="text-5xl font-black tracking-[-.06em]">Fantasy lab</h1><div className="mt-8 grid gap-4 md:grid-cols-3">{['Rankings','Start/Sit','Waivers','Player comparison','Matchup grades','Injury impact','Projected fantasy points'].map(x=><GlowCard key={x} className="p-6"><h2 className="text-xl font-bold">{x}</h2><p className="mt-2 text-gray-400">Premium shell connected to the fantasy endpoint with demo fallback.</p></GlowCard>)}</div>{f.items.map(i=><GlowCard key={i.player} className="mt-4 p-4">#{i.rank} {i.player} · {i.position} · {i.projected} pts · {i.grade}</GlowCard>)}</main>}
