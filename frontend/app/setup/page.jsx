'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
import GlowCard from '@/components/ui/GlowCard';
import SmartBetSportsLogo from '@/components/branding/SmartBetSportsLogo';
import {api} from '@/lib/api';
export default function Setup(){const router=useRouter();const[form,setForm]=useState({name:'',email:'',password:'',confirm:'',setupCode:''});const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setError('');if(form.password!==form.confirm){setError('Passwords do not match.');return}setLoading(true);try{await api.auth.bootstrapAdmin({name:form.name,email:form.email,password:form.password,setupCode:form.setupCode});router.replace('/internal/operations')}catch(ex){setError(ex.message||'Unable to complete initial setup.')}finally{setLoading(false)}}const input='focus-ring rounded-xl border border-white/10 bg-white/5 px-4 py-2.5';return <main className="mx-auto flex min-h-[calc(100vh-140px)] max-w-md items-center px-6 py-8"><GlowCard className="w-full p-6"><SmartBetSportsLogo size={34}/><h1 className="mt-4 text-3xl font-black">One-time dashboard setup</h1><p className="mt-2 text-sm text-slate-400">Creates or upgrades the first internal operator. The setup code and account details are sent only in the encrypted request body.</p><form onSubmit={submit} className="mt-5 grid gap-3"><label className="grid gap-1 text-sm font-semibold">Name<input required autoComplete="name" className={input} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label className="grid gap-1 text-sm font-semibold">Email<input required type="email" autoComplete="email" className={input} value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="grid gap-1 text-sm font-semibold">Password<input required minLength={8} type="password" autoComplete="new-password" className={input} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><label className="grid gap-1 text-sm font-semibold">Confirm password<input required minLength={8} type="password" autoComplete="new-password" className={input} value={form.confirm} onChange={e=>setForm({...form,confirm:e.target.value})}/></label><label className="grid gap-1 text-sm font-semibold">One-time setup code<input required minLength={32} type="password" autoComplete="off" className={input} value={form.setupCode} onChange={e=>setForm({...form,setupCode:e.target.value})}/></label>{error&&<p className="rounded-xl bg-red-500/10 p-3 text-sm text-red-200">{error}</p>}<button disabled={loading} className="btn btn-primary disabled:opacity-60">{loading?'Setting up…':'Create Dashboard Account'}</button></form></GlowCard></main>}
